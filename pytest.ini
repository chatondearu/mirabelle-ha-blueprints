[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = --import-mode=importlib
markers =
    smoke: Fast blueprint load tests (use_blueprint setup only)
    behavior: Behavioral automation/script scenario tests

filterwarnings =
    ignore::DeprecationWarning
