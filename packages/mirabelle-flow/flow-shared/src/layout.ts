/** Default graph layout spacing (pixels). */
export const FLOW_LAYOUT = {
  startX: 80,
  startY: 80,
  horizontalGap: 280,
  verticalGap: 100,
  /** Vertical offset between config band (top) and execution flow (bottom). */
  configBandHeight: 220,
  configGroupWidth: 240,
  configChildStep: 52,
  configHeaderHeight: 56,
  configPadding: 20,
} as const
